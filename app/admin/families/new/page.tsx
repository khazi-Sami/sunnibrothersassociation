"use client"; /* eslint-disable @typescript-eslint/no-explicit-any */ import Link from "next/link"; import { useState } from "react";
export default function NewFamily(){const [form,setForm]=useState<any>({incomeRange:"PREFER_NOT_TO_SAY"});const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/admin/families",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,householdSize:form.householdSize?Number(form.householdSize):undefined})});const d=await r.json();if(!r.ok)return setError(d.error);location.href=`/admin/families/${d.family.id}`}return <main className="interior-page"><div className="interior-shell"><Link href="/admin/families" className="site-btn-secondary">Families</Link><h1 style={{marginTop:24}}>Create family</h1><form onSubmit={submit} className="interior-panel" style={{display:"grid",gap:12,marginTop:18}}>{["familyName","primaryGuardianName","primaryGuardianPhone","primaryGuardianRelationship","city","address","householdSize","primaryOccupation"].map(k=><label key={k} style={{display:"grid",gap:6}}><span>{k}</span><input required={k.includes("primary")||k==="city"} type={k==="householdSize"?"number":"text"} value={form[k]||""} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label style={{display:"grid",gap:6}}><span>Income range</span><select value={form.incomeRange} onChange={e=>setForm({...form,incomeRange:e.target.value})}>{["BELOW_10000","INR_10000_20000","INR_20000_30000","INR_30000_50000","INR_50000_100000","ABOVE_100000","PREFER_NOT_TO_SAY"].map(v=><option key={v}>{v}</option>)}</select></label>{error&&<p role="alert">{error}</p>}<button className="site-btn-primary">Create family</button></form></div></main>}
