"use client"; /* eslint-disable @typescript-eslint/no-explicit-any */ import { useEffect, useState } from "react"; import Link from "next/link";
export default function NotificationsPage(){const [items,setItems]=useState<any[]>([]);const load=()=>fetch("/api/admin/notifications").then(r=>r.json()).then(d=>setItems(d.notifications||[]));useEffect(()=>{load()},[]);async function mark(id?:string){await fetch("/api/admin/notifications",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(id?{id}:{all:true})});load()}return <main className="interior-page"><div className="interior-shell"><Link href="/admin" className="site-btn-secondary">Admin dashboard</Link><header className="dashboard-intro"><div><div className="site-section-label">Operations</div><h1>Notifications</h1><p>Recent account and profile activity.</p></div><button className="site-btn-secondary" onClick={()=>mark()}>Mark all read</button></header><section style={{display:"grid",gap:12}}>{items.map(n=><article key={n.id} className="interior-panel" style={{opacity:n.isRead?.6:1}}><h2>{n.title}</h2><p>{n.message}</p><small>{new Date(n.createdAt).toLocaleString()}</small>{!n.isRead&&<button className="site-btn-secondary" onClick={()=>mark(n.id)}>Mark read</button>}</article>)}</section></div></main>}
