import Link from "next/link"; import { requireRole } from "@/lib/roleAccess"; import { getPrisma } from "@/lib/prisma";
export default async function TeacherAttendance(){const session=await requireRole("TEACHER");const classes=await getPrisma().class.findMany({where:{teacherId:session.user.id},include:{course:{select:{title:true,_count:{select:{enrollments:true}}}},attendances:{select:{status:true}}},orderBy:{scheduledAt:"desc"},take:100});return <main className="interior-page"><div className="interior-shell"><Link href="/dashboard" className="site-btn-secondary">Dashboard</Link><header className="dashboard-intro"><div><div className="site-section-label">Teaching workspace</div><h1>Attendance</h1><p>Review and confirm attendance for your own classes.</p></div></header><section style={{display:"grid",gap:12}}>{classes.map(item=>{const marked=item.attendances.filter(row=>row.status!=="UNMARKED").length;return <article key={item.id} className="interior-panel"><h2>{item.title}</h2><p>{item.course.title} · {new Date(item.scheduledAt).toLocaleString()}</p><p>{marked}/{item.course._count.enrollments} marked</p><Link href={`/teacher/attendance/${item.id}`} className="site-btn-primary">Manage attendance</Link></article>})}</section></div></main>}
