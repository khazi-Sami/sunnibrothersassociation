import Link from "next/link"; import { requireRole } from "@/lib/roleAccess"; import { getPrisma } from "@/lib/prisma"; import { attendancePercentage } from "@/lib/attendance";
export default async function AttendancePage(){const session=await requireRole("STUDENT");const rows=await getPrisma().classAttendance.findMany({where:{studentId:session.user.id},include:{class:{select:{title:true,scheduledAt:true,course:{select:{title:true}}}}},orderBy:{class:{scheduledAt:"desc"}}});const percent=attendancePercentage(rows);return <main className="interior-page"><div className="interior-shell"><Link href="/dashboard" className="site-btn-secondary">Dashboard</Link><header className="dashboard-intro"><div><div className="site-section-label">Learning record</div><h1>My attendance</h1><p>Teacher-confirmed attendance for your enrolled classes.</p></div><span className="dashboard-role">{percent}%</span></header><section className="dashboard-grid"><article className="dashboard-card"><div><h2>Overall attendance</h2><strong style={{fontSize:42}}>{percent}%</strong></div></article></section><section style={{display:"grid",gap:12,marginTop:24}}>{rows.map(row=><article key={row.id} className="interior-panel"><h2>{row.class.title}</h2><p>{row.class.course.title} · {new Date(row.class.scheduledAt).toLocaleDateString()}</p><strong>{row.status}</strong>{row.notes&&<p>{row.notes}</p>}</article>)}</section></div></main>}
